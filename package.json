{
  "name": "news-reader",
  "private": true,
  "description": "Flipboard-like news reader with Express proxy for TheNewsApi",
  "scripts": {
    "server:install": "npm install --prefix server && npm install --prefix web",
    "server:dev": "npm run dev --prefix server",
    "web:dev": "npm run dev --prefix web",
    "dev": "concurrently -n server,web -c blue,green \"npm run server:dev\" \"npm run web:dev\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
